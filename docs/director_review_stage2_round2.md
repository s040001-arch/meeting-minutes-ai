# 段2前再レビュー Round2(ディレクターClaude → Claude Code)

## 前提
docs/director_review_stage2.md の3点ゲートは完了。タスク1(合っています#B残存)・
タスク2(テスト緑化)は承認済み。ただしタスク3(コミット分割)で、3bのコードが
段2デプロイ対象から漏れる重大問題が判明した。以下を自律で進める。

## 最重要: 3bコードが段2デプロイ対象から外れている問題
段2の方針(2-d)は「本番デプロイ + Railwayで SEMANTIC_INTEGRITY_GATE_ENABLED=on に
して、3bを効かせた状態で①④をapplyする」こと。3bは①④を自動で本文に反映する際の
安全網であり、段2の大前提。
ところが現状のコミット分割では、semantic_integrity_gate.py(3bのモデルID更新=
claude-sonnet-4-6・missing_from_llm保留・「正しい語が文脈で一意か」判定)が、
段2デプロイ対象の(a)6565c05でなく、対象外の(b)527c8ad に入っている。
このまま(a)だけをデプロイして SEMANTIC=on にすると、本番の semantic_integrity_gate.py
はRailway上の古いコードのままになり、廃止済みの旧Sonnet4(claude-sonnet-4-20250514)を
叩いて全APIエラー → 設計上ジョブ停止。デプロイ直後に本番が止まる。最悪、古いコードが
エラーを握りつぶす版なら「安全網が無効なのに効いて見える」状態が再現する。

### 対処: 段2デプロイ単位を、3bを含む正しい単位に再定義する
- 「①④のapply本番に必要な本番コード一式」を洗い出す。最低限:
  edit_proposal_schema.py(大原あ是正・routing) / contextual_editor.py(idempotentガード) /
  editor_apply.py(normalize) に加え、semantic_integrity_gate.py(3b現行ID・
  missing_from_llm保留・一意性判定)が必須。さらにこれらが依存する
  fact_integrity_gate.py / pinpoint_answer_apply.py 等も本番に揃っているか確認。
- Railway上の現行コードと、これらの差分を確認。本番に何が既にあり、今回何を出すのかを明示。
- コミットを切り直す、または段2デプロイのチェリーピック範囲を、3bを含む形で再定義する。
- 「①④applyに必要な本番コード一式」が漏れなくデプロイ範囲に入ることを保証する。

## 段2前にもう1点: ③件数の正を確定する(67/71/69で揺れている)
fixtureは67、再exportすると71、再バンドル後は69、と③の件数が揺れている。テストは67に
合わせたが、正しい値がどれか未確定。段2(①④のapply)はブロックしないが、③回答フロー
(B48・C10)に進む前に確定が要る。なぜ3つの値が出るのか(A8回答済み8件の扱い・再バンドルの差)を
切り分け、③の正しい件数を1つに定める。これは段2と並行で調査してよい。

## docs更新(自律でやってよい)
- docs/phase10_handoff.md を現状(3コミット・段2デプロイ範囲は要再定義・3b所在問題)に更新
- docs/director_review_stage2.md(前回の指示書)をコミットして履歴として残す

## 自律で進めてよい範囲(承認不要)
上記のコード差分確認・コミット再構成・③件数調査・docs更新・テスト緑維持。
完了したら、再構成後のコミット構成・段2デプロイ範囲・③件数の結論をまとめて報告。

## 止まって相原に聞く
相原にしか答えがない事実判断が出たときだけ、一問にまとめて聞く。推測で進めない。

## 絶対に踏み込まない(相原のGOまで実行しない)
- 段2の本番デプロイ + Railway SEMANTIC_INTEGRITY_GATE_ENABLED=on
- 不変境界線: run_job_once CLI契約・exit code / Step4.2 learned_corrections /
  Drive検知・ジョブ起動本体 / スキーマ後方互換

## 判断基準(絶対遵守)
事実の正しさ > 意味が通る > 工数最小 / 鉄の掟(事実は①④禁止→②③) /
本文はpinpointのみ・LLMに織り込ませない / 穴塞ぎでなく逆算 / 1Phase=1デプロイ・混ぜない

## 完了後
コミット再構成・③件数の結果をまとめて、ディレクターClaudeの再レビューに出す。
段2は再レビューGO + 相原GOの後。
